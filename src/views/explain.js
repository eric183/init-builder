// 说明-------记录代码目录可能不规范合理的地方
// 1. 手机号验证等 放在utils/validate  

// 2. 弹窗写在当前页面组件中  author,company，VistorCompany，VistorAccount，stolist，category，indlist，more，sysUser

// 3. 当前分支下的require 没有改成import

// 4. 当前文件夹下写的的ui组件模板/js   electric,water,gas，advmanage，odflist，cableslist，eatList，customizedDataCount，roomPersonList，roomPersonRecord

// 5. 引用当前文件夹外部的ui模板/js    department，MessagePush，personList，visitorUser，discernRecord，blacklist，faceGroup，faceDeviceList，roomOrder，park，
//         oderlist，orderList，dining，superGroup，workOrderList，bill，superDepartment

// 6. 富文本没有抽出公共的组件，富文本引用的js写在当前文件夹下    infomation，activityList

// 7. 文件夹下面相同的详情页面           repair，complain，consult